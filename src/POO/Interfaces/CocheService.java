package POO.Interfaces;

import POO.Coche;

public interface CocheService {
    // Las interfaces se pueden declarar metodos de una clase normal que se implemente a una clase
    public Coche crearCoche(); // este es un metodo que se crea pero no tiene cuerpo, por lo que no tiene problema

}
